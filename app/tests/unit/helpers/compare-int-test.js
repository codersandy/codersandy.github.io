
import { compareInt } from 'testapp/helpers/compare-int';
import { module, test } from 'qunit';

module('Unit | Helper | compare int');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = compareInt([42]);
  assert.ok(result);
});

